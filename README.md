# @function-planner/shared

Shared TypeScript types, Zod schemas, and plan utilities for [Function Planner](https://github.com/MoravianUniversity/function-planner).

Used by the monorepo server/client and kept as a git submodule at `packages/shared`.

## Scripts

```bash
npm install
npm run build
npm test
```
